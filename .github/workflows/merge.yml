name: Merge

on:
  workflow_dispatch:

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check repository branch setup
        run: |
          echo "This repository uses main as its active branch."
          echo "The old production-branch merge is no longer needed."
